# homework1

Accessibility Code Refined

```
Source code has semantic HTML elements

The HTML elements are structured in a logical way independent of styling and positioning

The image elements have alt attributes

The heading attributes fall in sequential order (a reset.css is included)

The title element is concise and descriptive

All code contains comment sections
```